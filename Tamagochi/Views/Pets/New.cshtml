<!DOCTYPE html>
<html>
  <head>
    <meta charset='utf-8'>
    <title>TAMAGOCHI</title>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
    <link rel="stylesheet" href="/css/styles.css">
  </head>
  <body>
    <h1>Add a Pet to take care of him</h1>

    <h3>Add a new pet to @Model.GetName()</h3>

    <form action='/owners/@Model.GetId()/pets' method='post'>
      <input id='ownerId' name='ownerId' type='hidden' value='@Model.GetId()'>
      <label for='petBreed'>Pet breed</label>
      <input id='petBreed' name='petBreed' type='text'>
      <button type='submit'>Add a Pet to care of</button>
    </form>

  </body>
</html>
